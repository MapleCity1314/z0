[package]
name = "z0-boss-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = "1"
z0-boss-auth = { path = "../auth" }
z0-boss-core = { path = "../core" }
z0-boss-live = { path = "../live" }
