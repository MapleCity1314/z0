{
  "name": "z0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:web": "pnpm --filter @z0/web dev",
    "dev:api": "pnpm --filter @z0/api dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "check-types": "turbo run check-types",
    "typecheck": "pnpm check-types",
    "check": "turbo run lint check-types"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "turbo": "^2.8.17",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.11.1"
  }
}
