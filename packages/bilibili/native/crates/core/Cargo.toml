[package]
name = "z0-bilibili-core"
version = "0.1.0"
edition = "2021"

[dependencies]
md5 = "0.7"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
urlencoding = "2"
